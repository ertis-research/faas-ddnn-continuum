apiVersion: v1
kind: ConfigMap
metadata:
  name: kafka-connector-openfaas-cloud-input-config
data:
  config.json: |-
    {
      "concurrency": 1024,
      "topic": "openfaas.inference.cloud-input",
      "groupid": "openfaas.inference.cloud-input",
      "brokers": [
        "kafka-0.kafka-headless.kafka:9092",
        "kafka-1.kafka-headless.kafka:9092"
      ],
      "callback": {
        "url": "http://gateway.openfaas:8080/function/cloud",
        "options": {
          "method": "POST",
          "keepalive": true
        }
      }
    }

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka-connector-openfaas-cloud-input
  labels:
    name: kafka-connector-openfaas-cloud-input
    reloader.stakater.com/auto: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      name: kafka-connector-openfaas-cloud-input
  template:
    metadata:
      labels:
        name: kafka-connector-openfaas-cloud-input
    spec:
      containers:
        - name: connector
          image: 10.10.32.232:30501/kafka-connector:0.0.16
          imagePullPolicy: Always
          env:
            - name: KAFKAC_CLIENTID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          volumeMounts:
            - name: config
              mountPath: "/etc/kafkac"
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: kafka-connector-openfaas-cloud-input-config
